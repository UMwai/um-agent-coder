# Palette's UX Journal

## 2024-05-22 - Initial Setup
**Learning:** This is a Python CLI application, so UX improvements will focus on terminal output, prompts, and feedback mechanisms rather than HTML/CSS.
**Action:** Look for opportunities in `cli.py`, `utils/`, and interaction points in `orchestrator/`.
